// MIGRATION SCRIPT (DEPRECATED)
// This file used to move todos from the global `todos` collection into per-user
// collections. Per project requirements we now keep all todos in the single
// `todos` collection. Do NOT run this script.

console.log('migrate_todos.js is deprecated. Per-user collections are disabled.');
